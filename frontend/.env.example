# Cible du proxy Vite pendant le développement.
VITE_DEV_API_TARGET=http://127.0.0.1:3001

# Optionnel : URL absolue de l’API si le frontend est hébergé séparément.
# Laisser vide pour utiliser l’API du même serveur (mode recommandé).
VITE_API_BASE_URL=
